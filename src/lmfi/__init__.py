"""lmfi — Failure by Interference (RASTEER).

Mechanistic-interpretability analysis and a ranking/steering method on the
balanced-parentheses task. See CLAUDE.md for the migration plan and structure.

Modules (io.py, models/, analysis/, steering/, viz/) are populated as each
experiment is migrated; this package is intentionally near-empty during scaffold.
"""
